node_modules/
contracts/out/
*.log
logs/
.DS_Store

# A deployment this machine paid for: the contract address and each holder's
# anchored run. Local to whoever ran it, and meaningless on another chain.
.bridge-state-*.json

# The relay's compiled output.
fee-relay/dist/

# Anything holding a key or a password. Nothing in this repo should, but the
# rule is cheaper than the incident.
.env
.env.*
!.env.example
*.seed
RECOVERY.txt
*.bak

# Recording artefacts. The scripts that produce them are tracked; the video is
# a build output, and a 20MB mp4 in git history is there forever.
demo/out/

# Build output. 62MB, most of it the prover keys copied in for static hosting,
# and every byte reproducible with `npm run build`.
app/dist/

# Vercel's local project link.
.vercel/
